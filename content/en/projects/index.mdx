---
title: Project List
description: A learning path from first maker build to production-minded IoT devices with PxServ and ESP32.
asIndexPage: true
---

import { Callout } from "nextra/components";

# Project List

<div className="docs-section-heading">
  <div>
    This section takes you from your first cloud-connected sensor to clear,
    production-minded device patterns through a practical learning sequence.
    Every project includes objectives, parts, a wiring table, PxServ keys,
    complete firmware examples, a test procedure, and troubleshooting notes.
  </div>
</div>

<div className="docs-feature-grid">
  <a className="docs-feature-card" data-tone="arduino" href="/en/projects/beginner/">
    <div className="docs-feature-top"><span className="docs-feature-icon">01</span><span className="docs-feature-arrow">→</span></div>
    <h3>Beginner · 10 Projects</h3>
    <div className="docs-feature-copy">Build your base with GPIO, basic sensors, setData(), and getData().</div>
  </a>

<a
  className="docs-feature-card"
  data-tone="typescript"
  href="/en/projects/intermediate/"
>
  <div className="docs-feature-top">
    <span className="docs-feature-icon">02</span>
    <span className="docs-feature-arrow">→</span>
  </div>
  <h3>Intermediate · 10 Projects</h3>
  <div className="docs-feature-copy">
    Move to multi-task loops, remote commands, calibration, and alarms.
  </div>
</a>

  <a className="docs-feature-card" data-tone="rust" href="/en/projects/advanced/">
    <div className="docs-feature-top"><span className="docs-feature-icon">03</span><span className="docs-feature-arrow">→</span></div>
    <h3>Advanced · 5 Projects</h3>
    <div className="docs-feature-copy">Practice safe states, hysteresis, richer telemetry, maps, and OTA flows.</div>
  </a>
</div>

<div className="docs-section-heading docs-section-heading--compact">
  <h2>One path across all project levels</h2>
  <div>
    The sequence is intentionally balanced: 10 beginner, 10 intermediate, and 5
    advanced projects. You can follow end-to-end or jump directly to the level
    that matches your current needs.
  </div>
</div>

<Callout type="info">
  <strong>Common development board</strong>
  <div>
    Examples target an ESP32 DevKit V1 in Arduino IDE. PxServ also supports
    ESP8266, but pin names, ADC ranges, and peripheral APIs must be adapted.
  </div>
</Callout>

## One-time setup

1. Create a PxServ project and copy its project API key.
2. Install the ESP32 board package in Arduino IDE.
3. Install PxServ `1.3.0` or newer through Library Manager.
4. Replace `WIFI_SSID`, `WIFI_PASSWORD`, and `PXSERV_API_KEY` in each sketch.
5. Select your exact ESP32 board and serial port.

Never publish your API key. In a real product, keep credentials in a separate configuration file excluded from version control.

## Electrical safety

- ESP32 GPIO uses **3.3 V logic**; never apply a 5 V signal directly.
- Level-shift 5 V outputs such as HC-SR04 ECHO before connecting them to ESP32.
- Power pumps, motors, and servos from a suitable external supply and share GND.
- Relay examples are for safe, low-voltage DC loads only. Do not work with mains voltage.
- Confirm your module's pinout and voltage limits in its manufacturer documentation.

## PxServ key model

PxServ sends values as strings. Keys use only a short project prefix—such as `buzzer/command`, `greenhouse/temperature`, or `gps/status`—without a skill-level prefix.

```cpp
client.setData("temperature-humidity/temperature", String(temperature, 1));
```

The series is based on the [official PxServ Arduino source](https://github.com/pxsty0/pxserv.arduino), the [Arduino language reference](https://docs.arduino.cc/language-reference/), [Arduino-ESP32 documentation](https://docs.espressif.com/projects/arduino-esp32/en/latest/), and the sensor references linked in each project.

Start with [Cloud Hello](/en/projects/beginner/01-cloud-hello/).
